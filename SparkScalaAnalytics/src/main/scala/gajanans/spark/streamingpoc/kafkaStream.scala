package gajanans.spark.streamingpoc

import org.apache.spark.SparkContext

object kafkaStream {
  def main(args: Array[String]): Unit = {
    val sc = new SparkContext("local","Spark Kafka Stream");
//    val streamingInputDF = sc.
  }
}